(function(){
  'use strict';
  var KEY='gamoravet_full_v8';
  var editId=0;
  function $(id){return document.getElementById(id)}
  function load(){try{return JSON.parse(localStorage.getItem(KEY))||{}}catch(e){return{}}}
  function save(s){localStorage.setItem(KEY,JSON.stringify(s))}
  function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,function(m){return{'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[m]})}
  function petName(state,id){var p=(state.pets||[]).find(function(x){return Number(x.id)===Number(id)});return p?p.name:'pet'}
  function notifId(id,offset){return Math.abs((Number(id)%2000000000)+offset)}
  function bridge(){return window.GamoraVetAndroid||null}
  function cancelMedAlarm(id){try{var b=bridge();if(b)b.cancelNotification(notifId(id,100000000))}catch(e){}}
  function nextMedMs(time){var p=String(time||'').split(':'),d=new Date();d.setHours(Number(p[0]||0),Number(p[1]||0),0,0);if(d.getTime()<=Date.now())d.setDate(d.getDate()+1);return d.getTime()}
  function scheduleMed(m,state){try{var b=bridge();if(b&&m.time)b.scheduleNotification(notifId(m.id,100000000),nextMedMs(m.time),'GamoraVet • Hora do medicamento',m.name+(m.dose?' — '+m.dose:'')+' para '+petName(state,m.petId))}catch(e){}}
  function dateKey(d){var x=d||new Date();return x.getFullYear()+'-'+String(x.getMonth()+1).padStart(2,'0')+'-'+String(x.getDate()).padStart(2,'0')}
  function nowText(){return new Date().toLocaleString('pt-BR')}
  function toast(msg){var t=$('toast');if(!t)return;t.textContent=msg;t.style.display='block';setTimeout(function(){t.style.display='none'},1800)}
  function currentConfirmation(state,medId){var day=dateKey();return (state.medAdministrations||[]).find(function(a){return Number(a.medId)===Number(medId)&&a.dateKey===day})}
  function renderHistoryFromStorage(){var list=$('historyList');if(!list)return;var state=load(),items=(state.history||[]).slice().sort(function(a,b){return Number(b.id||0)-Number(a.id||0)});list.innerHTML=items.map(function(x){return '<div class="card"><b>'+esc(x.title||'Registro de saúde')+'</b><div class="small">'+esc(x.date||'')+'</div><div>'+esc(x.note||'')+'</div></div>'}).join('')||'<div class="card empty">Nenhum registro de saúde.</div>';if($('rHistory'))$('rHistory').textContent=items.length}
  function ensureForm(){var saveBtn=$('saveMed');if(!saveBtn)return false;if(!$('medEditId')){var hidden=document.createElement('input');hidden.type='hidden';hidden.id='medEditId';saveBtn.parentNode.insertBefore(hidden,saveBtn)}if(!$('cancelMedEdit')){var cancel=document.createElement('button');cancel.id='cancelMedEdit';cancel.className='btn ghost';cancel.type='button';cancel.textContent='Cancelar edição';cancel.style.display='none';cancel.style.marginLeft='8px';saveBtn.insertAdjacentElement('afterend',cancel);cancel.onclick=clearForm}saveBtn.onclick=saveMedication;return true}
  function clearForm(){editId=0;if($('medEditId'))$('medEditId').value='';if($('medName'))$('medName').value='';if($('medDose'))$('medDose').value='';if($('medTime'))$('medTime').value='';if($('saveMed'))$('saveMed').textContent='Salvar e ativar lembrete';if($('cancelMedEdit'))$('cancelMedEdit').style.display='none'}
  function saveMedication(){if(window.GamoraVetMeds12&&window.GamoraVetMeds12.save)return window.GamoraVetMeds12.save();var state=load();state.meds=state.meds||[];state.audit=state.audit||[];if(!(state.pets||[]).length){if(window.showView)window.showView('pets');return}var name=$('medName').value.trim(),dose=$('medDose').value.trim(),time=$('medTime').value,petId=Number($('medPet').value);if(!name||!time){toast('Informe medicamento e horário');return}var id=Number(($('medEditId')&&$('medEditId').value)||editId||0);if(id){var m=state.meds.find(function(x){return Number(x.id)===id});if(!m)return;cancelMedAlarm(m.id);m.petId=petId;m.name=name;m.dose=dose;m.time=time;scheduleMed(m,state);state.audit.unshift({id:Date.now(),at:nowText(),action:'Medicação editada: '+name})}else{var med={id:Date.now(),petId:petId,name:name,dose:dose,time:time};state.meds.push(med);scheduleMed(med,state);state.audit.unshift({id:Date.now()+1,at:nowText(),action:'Medicação cadastrada: '+name})}save(state);clearForm();renderMeds()}
  function renderMeds(){if(window.GamoraVetMeds12)return;var list=$('medList');if(!list)return;var state=load(),meds=(state.meds||[]).slice().sort(function(a,b){return String(a.time).localeCompare(String(b.time))});list.innerHTML=meds.map(function(m){return '<div class="card"><div><b>💊 '+esc(m.name)+'</b><div class="small">'+esc(petName(state,m.petId))+' • '+esc(m.dose)+' • '+esc(m.time)+'</div></div></div>'}).join('')||'<div class="card empty">Nenhuma medicação.</div>'}
  function start(){if(!ensureForm()){setTimeout(start,100);return}renderMeds();renderHistoryFromStorage();document.addEventListener('click',function(e){if(window.GamoraVetMeds12)return;var med=e.target.closest&&e.target.closest('[data-view="meds"],button[onclick*="showView(\'meds\')"]');if(med)setTimeout(renderMeds,30)},true)}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start);else start();
})();